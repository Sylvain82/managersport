{% extends 'base.html.twig' %}

{% block title %}Finance{% endblock %}

{% block body %}
    <h1>Finance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ finance.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ finance.type ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ finance.montant }}</td>
            </tr>
            <tr>
                <th>Justification</th>
                <td>{{ finance.justification }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('finance_index') }}">back to list</a>

    <a href="{{ path('finance_edit', {'id': finance.id}) }}">edit</a>

    {{ include('finance/_delete_form.html.twig') }}
{% endblock %}
