{% extends 'base.html.twig' %}

{% block title %}Les équipes{% endblock %}

{% block body %}
<h1 class="m-3">Calendrier</h1>

<div class="container">

    {%  for equipe in  equipe %}

            <div class="col-md-4 mt-5 mb-3">
                <div class="card">

                    <div class="card-body">
                        <a href="{{ path('equip',  {'id': equipe.id} ) }}" class="btn btn-primary">Fiche :  {{ equipe.nom }} </a>

                    </div>
                </div>

            </div>

    {% endfor %}

{% endblock %}
